#include <bits/stdc++.h>

#define N 10000
using namespace std;

int n, koszt[N], odl[N], result;
int kosztL, kosztP, ileL, ileP, odlL, odlP;

int main() {
    cin >> n;
    int i;
    for (i = 0; i < n; i++) {
        scanf("%d %d", &koszt[i], &odl[i]);
    }
    for (i = 1; i < n; i++) {
        // rezygnacja
    }

}